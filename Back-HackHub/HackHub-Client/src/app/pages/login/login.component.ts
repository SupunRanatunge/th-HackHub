import { Component } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'app-login',
  templateUrl: `login.component.html`,
})
export class LoginComponent  {
  loginClick() {

  }
}
